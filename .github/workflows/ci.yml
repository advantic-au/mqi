name: Continuous Integration

on:
  workflow_dispatch:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
  schedule: [cron: "40 4 * * 5"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

permissions: {}

jobs:
  client_check:
    strategy:
      fail-fast: false
      matrix:
        version:
          - client-version: 9.2.0.31
            feature: mqc_9_2_0_0
          - client-version: 9.3.0.27
            feature: mqc_9_3_0_0
          - client-version: 9.3.5.1
            feature: mqc_9_3_5_0
          - client-version: 9.4.2.0
            feature: mqc_9_4_2_0
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: advantic-au/action-mq-client@stable
        id: mq-client
        with:
          client-version: ${{ matrix.version.client-version }}
      - name: Install stable
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "build"
      - name: Check
        run: cargo check --features ${{ matrix.version.feature }},mqai,dlopen2
        env:
          MQ_HOME: ${{ steps.mq-client.outputs.client-install-path }}

  test_server:
    name: "MQ server integration test"
    needs: test
    runs-on: ubuntu-24.04
    services:
      mq:
        image: icr.io/ibm-messaging/mq:latest
        env:
          MQ_ENABLE_EMBEDDED_WEB_SERVER: false
          LICENSE: accept
          MQ_APP_PASSWORD: app
        ports:
          - 1414:1414
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: advantic-au/action-mq-client@stable
        id: mq-client
        with:
          client-version: latest
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
      - uses: taiki-e/install-action@v2.49.0
        with:
          tool: nextest
      - name: Test
        run: cargo nextest run --profile ci --no-default-features --features runnable,link,mqai,mqc_9_4_2_0
        env:
          MQ_HOME: ${{ steps.mq-client.outputs.client-install-path }}
          MQSERVER: DEV.APP.SVRCONN/TCP/localhost(1414)
          MQ_USER: app
          MQ_PASSWORD: app

  test:
    name: "Test rust ${{matrix.toolchain}}"
    strategy:
      fail-fast: false
      matrix:
        toolchain: [nightly, beta, stable]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: advantic-au/action-mq-client@stable
        id: mq-client
        with:
          client-version: latest
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{matrix.toolchain}}
      - name: Enable type layout randomization
        run: echo RUSTFLAGS=${RUSTFLAGS}\ -Zrandomize-layout >> $GITHUB_ENV
        if: matrix.toolchain == 'nightly'
      - uses: taiki-e/install-action@v2.49.0
        with:
          tool: nextest
      - name: Test
        run: |
          cargo nextest run --profile ci --all-features
          cargo test --all-features --doc
        env:
          RUSTFLAGS: -Dwarnings
          MQ_HOME: ${{ steps.mq-client.outputs.client-install-path }}

  miri:
    runs-on: ubuntu-24.04
    needs: test
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly
          components: miri
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "build"
      - uses: taiki-e/install-action@v2.49.0
        with:
          tool: nextest
      - name: Execute miri tests
        env:
          MIRIFLAGS: -Zmiri-strict-provenance -Zmiri-env-forward=PROPTEST_DISABLE_FAILURE_PERSISTENCE -Zmiri-env-forward=PROPTEST_CASES
          PROPTEST_CASES: 64
          PROPTEST_DISABLE_FAILURE_PERSISTENCE: true
        run: cargo miri nextest run --profile ci --no-default-features --features mock,mqai,mqc_9_4_2_0

  incremental_mutants:
    runs-on: ubuntu-24.04
    needs: test
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: advantic-au/action-mq-client@stable
        id: mq-client
        with:
          client-version: latest
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
      - name: Relative diff
        run: |
          git branch -av
          git diff origin/"${GITHUB_BASE_REF}".. > git.diff
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2.49.0
        with:
          tool: cargo-mutants
      - name: Mutant testing
        run: |
          cargo mutants --no-shuffle -vV --in-diff git.diff --all-features
        env:
          MQ_HOME: ${{ steps.mq-client.outputs.client-install-path }}
        continue-on-error: true
      - name: Artefact mutants-incremental.out
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mutants-incremental.out
          path: mutants.out

  rustfmt:
      runs-on: ubuntu-24.04
      steps:
        - uses: actions/checkout@v4
          with:
            persist-credentials: false
        - name: Install stable
          uses: dtolnay/rust-toolchain@master
          with:
            toolchain: stable
            components: rustfmt
        - name: Lint with rustfmt
          run: cargo fmt -- --check

  clippy:
    strategy:
      fail-fast: false
      matrix:
        toolchain: [beta, stable]
        features:
          - ""
          - "--no-default-features"
          - "--all-features"
        include:
          - features: "--no-default-features"
            combination:
              - ""
              - "--features dlopen2"
              - "--features link"
              - "--features link,mqai"
              - "--features link,exits"
              - "--features dlopen2,mqai"
              - "--features dlopen2,exits"
    runs-on: ubuntu-24.04
    permissions:
      checks: write
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: advantic-au/action-mq-client@stable
        id: mq-client
        with:
          client-version: latest
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.toolchain }}
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "build"
      - name: Run clippy
        uses: auguwu/clippy-action@1.4.0
        env:
          RUSTFLAGS: -Dwarnings
          MQ_HOME: ${{ steps.mq-client.outputs.client-install-path }}
        with:
          check-args: --tests --examples ${{ matrix.features }} ${{ matrix.combinations }}
          token: ${{secrets.GITHUB_TOKEN}}

  minimum:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: advantic-au/action-mq-client@stable
        id: mq-client
        with:
          client-version: latest
      # Re-resolve Cargo.lock with minimal versions
      - uses: dtolnay/rust-toolchain@nightly # -Z option only available on nightly
      - run: cargo generate-lockfile -Z minimal-versions
      # Now verify that `cargo check` works with respect to the oldest possible
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85
      - run: cargo check --locked --all-features
        env:
          RUSTFLAGS: -Dwarnings
          MQ_HOME: ${{ steps.mq-client.outputs.client-install-path }}

  docs:
    runs-on: ubuntu-24.04
    env:
      RUSTDOCFLAGS: -Dwarnings
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly
      - uses: taiki-e/install-action@v2.49.0
        with:
          tool: cargo-docs-rs
      - run: cargo docs-rs

  clippy_outcome:
    if: ${{ always() }}
    runs-on: ubuntu-24.04
    name: Clippy outcome
    needs: [clippy]
    steps:
      - run: |
          result="${NEEDS_RESULT}"
          if [[ $result == "success" || $result == "skipped" ]]; then
            exit 0
          else
            exit 1
          fi
        env:
          NEEDS_RESULT: ${{ needs.clippy.result }}
